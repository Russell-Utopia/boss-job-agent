# BOSS 自动投递助手产品方案

版本：v1.2

形态：Web 交互

目标用户：陈俊州

文档状态：产品方案

## 1. 文档范围

本文只定义用户可感知的产品行为，包括：

- 用户如何进入和结束一次投递任务；
- 页面需要展示哪些信息；
- 哪些情况自动处理，哪些情况打断用户；
- 登录、资料缺失、验证码、暂停、恢复和异常退出时的交互；
- 正常完成与提前结束时如何生成报告。

本文不包含技术架构、组件选型、模型选型、浏览器控制方式、状态存储方式及具体实现方案。

## 2. 产品定义

BOSS 自动投递助手是一个面向个人使用的专用求职工具。

用户打开产品后，助手自动读取 BOSS 在线简历中的求职期望，按照其中填写的岗位、城市和其他搜索条件持续搜索。搜索结果不再进行二次岗位匹配，直接发起沟通并发送已经确定的固定招呼语，不需要用户逐个确认。

产品默认保持安静。只有登录失效、必要信息缺失、验证码、安全限制或任务无法继续时，才要求用户介入。

## 3. 产品目标

### 3.1 核心目标

1. 用户打开产品后即可自动开始一次投递任务。
2. 以 BOSS 在线简历作为唯一求职资料来源，不在助手内维护第二份资料。
3. 在用户不持续盯着页面的情况下，完整搜索在线简历中的全部意向岗位和目标城市。
4. 在不突破用户授权边界的前提下，对搜索结果进行持续海投。
5. 让用户随时知道任务是否仍在运行、为什么暂停、是否真正完成。
6. 任务结束后提供可核对的岗位级报告。

### 3.2 非目标

v1 不提供以下能力：

- 不要求用户上传附件简历；
- 不主动向招聘者发送在线简历或附件；
- 不代替用户回复招聘者；
- 不进行后续追问或持续聊天；
- 不让用户逐个岗位确认是否沟通；
- 不对搜索结果进行岗位相关度判断、评分或分级；
- 不根据工作年限、职级名称、技术栈、职责或经历相似度筛选岗位；
- 不绕过验证码、安全验证、平台限制或今日沟通上限；
- 不跨任务、跨天长期保存职位与公司记录。

## 4. 核心交互原则

### 4.1 自动优先

能从当前状态和在线简历中确定的事情直接执行，不要求用户确认。

例如：

- 已登录时直接读取在线简历；
- 在线简历信息完整时直接开始投递；
- 用户在浏览器完成登录或验证码后自动恢复；
- 短暂页面异常自动重试；
- 搜索范围全部完成后自动生成报告。

### 4.2 单一资料来源

意向岗位、目标城市、期望薪资和求职类型均来自 BOSS 在线简历中的求职期望。

助手不提供临时覆盖值，也不在产品内形成第二份求职资料。资料缺失或需要修改时，引导用户直接更新 BOSS 在线简历；检测到更新后自动继续。

### 4.3 使用标准 Web 交互

有固定答案范围时，使用单选项、按钮或菜单，不要求用户输入命令或固定口令。

所有选择均遵循常规 Web 交互：鼠标或触控可直接点击，键盘可用 Tab 移动焦点、方向键切换单选项、Enter 确认主要操作。

### 4.4 打断必须有明确原因

任何暂停用户流程的页面都必须回答三个问题：

1. 发生了什么；
2. 用户现在需要做什么；
3. 完成后是否会自动继续。

### 4.5 完成必须可证明

页面不能因为处理完当前可见的一批职位就显示“已完成”。

只有所有目标城市和全部搜索范围都完成后，才显示正常完成。若因今日上限、用户停止或异常而结束，必须明确显示“提前结束”以及尚未完成的范围。

## 5. 页面结构

产品只有一个主任务页面，不设置复杂导航。

### 5.1 顶部栏

持续展示：

- 产品名称；
- 当前任务状态；
- “暂停/继续”操作；
- “结束任务”操作。

当任务已经完成或结束时，不再显示暂停和结束操作。

### 5.2 主内容区

根据任务状态展示当前最重要的信息：

- 运行中：当前搜索范围、总体进度、已沟通数量、已跳过数量、最近一条有效动作；
- 需要用户介入：只展示当前问题与处理方式；
- 已完成或已结束：展示任务报告。

### 5.3 按需详情

岗位处理记录默认收起。用户点击“查看投递记录”后，才展示最近或完整的岗位明细。

默认页面不持续滚动技术日志，也不展示内部组件、模型、浏览器控制过程或搜索验证细节。

### 5.4 通知

当用户没有停留在产品页面时，只在以下情况发送一次系统通知：

- 需要重新登录；
- 需要处理验证码或安全验证；
- 任务因今日上限或异常提前结束；
- 任务正常完成。

不为每个职位、每次跳过或每次重试发送通知。用户点击通知后回到当前任务页面。

## 6. 完整用户流程

### 6.1 打开产品

1. 用户打开 Web 页面。
2. 页面立即进入“正在准备”状态，不展示“开始投递”按钮。
3. 产品检查当天是否已经存在任务：
   - 当天没有任务：创建新任务并继续；
   - 存在因刷新、关闭页面或异常退出而中断的任务：自动恢复原任务，不创建重复任务；
   - 任务由用户主动暂停：保持暂停并展示原进度，不自动继续；
   - 当天任务已经正常完成：直接展示完成报告，不自动重复投递；
   - 当天任务被用户主动结束：直接展示部分报告，由用户主动选择是否重新开始；
   - 当天已经达到平台上限或安全限制：继续展示停止原因，不提供当天重新开始入口。
4. 关闭或刷新 Web 页面不等于停止任务。用户必须点击“结束任务”才会主动终止本次投递。
5. 用户第一次使用时，页面需要明确提示一次：“关闭此页面不会停止正在进行的任务。”

### 6.2 登录与授权

产品分别识别 OpenAI 授权状态和 BOSS 登录状态。

#### OpenAI 授权有效

直接进入下一步，不打断用户。

#### OpenAI 授权失效

页面展示：

- 标题：“需要重新登录 OpenAI”；
- 说明：“完成登录后将自动继续本次任务”；
- 主操作：“前往登录”；
- 次操作：“结束任务”。

完成登录后自动恢复，不要求用户点击“我已登录”。

#### BOSS 已登录

直接读取在线简历。

#### BOSS 未登录

页面展示：

- 标题：“请在 Chrome 中完成 BOSS 登录”；
- 说明：“登录成功后将自动继续，不需要返回确认”；
- 状态：“正在检测浏览器登录状态”；
- 次操作：“重新打开 BOSS 页面”；
- 危险操作：“结束任务”。

页面不提供“已登录”按钮，也不要求用户输入确认文本。

### 6.3 读取在线简历

登录成功后，页面短暂显示“正在读取 BOSS 在线简历”。

产品只关心本次投递需要的资料：

- 一个或多个意向岗位；
- 目标城市；
- 期望薪资；
- 当前求职类型。

读取成功后：

- 信息完整：直接开始投递；
- 必要信息缺失：进入“在线简历待完善”，不在助手内询问或临时补充；
- 在线简历暂时无法读取：自动重试，仍失败时进入“无法读取在线简历”状态。

“无法读取在线简历”页面展示：

- 标题：“暂时无法读取 BOSS 在线简历”；
- 说明：“不会要求你重新上传简历”；
- 主操作：“重试”；
- 次操作：“结束任务”。

### 6.4 在线简历待完善

当意向岗位、目标城市、期望薪资或其他搜索必需信息缺失时，任务暂停并展示：

- 标题：“请先完善 BOSS 在线简历”；
- 缺失项列表，例如“意向岗位”“目标城市”“期望薪资”；
- 说明：“更新完成后将自动继续，不需要返回确认”；
- 主操作：“打开 BOSS 在线简历”；
- 次操作：“重新检测”；
- 危险操作：“结束任务”。

产品不提供临时填写表单，也不要求用户上传简历。

用户在 BOSS 中完成更新后，助手自动重新读取在线简历。资料完整则继续投递；仍有缺失项则刷新缺失项列表。

### 6.5 自动投递

进入运行状态后，不再逐个询问用户。

页面默认只展示：

- 状态：“自动投递中”；
- 当前范围，例如“广州 · 后端开发工程师”；
- 搜索进度，例如“2/6 个搜索范围”；
- 已沟通岗位数；
- 已跳过岗位数；
- 最近一条有效动作，例如“刚刚联系了云原科技 · 后端开发工程师”；
- 最近更新时间，例如“刚刚更新”；
- “查看投递记录”入口。

页面不默认展示：

- 每一次页面滚动；
- 每一个发现但尚未处理的职位；
- 内部重试次数；
- 每个跳过岗位的实时日志；
- 内部状态名称或技术诊断信息。

### 6.6 海投规则

#### 搜索范围

产品直接使用 BOSS 在线简历中填写的意向岗位进行搜索，不自行改写、扩展或替换岗位名称。在线简历包含多个意向岗位或多个目标城市时，依次完整覆盖所有组合。

#### 不做二次岗位判断

进入搜索结果的职位默认进入海投范围，不再判断：

- 岗位名称是否与简历经历相似；
- 工作年限是否匹配；
- 职级是否合适；
- 技术栈是否一致；
- 职责是否相关；
- 岗位是否属于“特别相关”或“普通相关”。

产品不展示岗位匹配分数，也不生成相关度标签。

#### 只保留操作性跳过

搜索结果只有在以下情况下不再发起沟通：

- 该职位已经沟通或已经投递；
- 同一职位在其他搜索范围中重复出现；
- 同一公司已经达到当天沟通数量限制；
- BOSS 页面明确不允许继续沟通；
- 当前操作失败且无法安全确认结果。

#### 多个招聘者发布相同或相近岗位

优先选择页面明确显示最活跃的招聘者。若没有可核验的活跃信息，报告中显示“无活跃信息”，不做主观推断。

#### 投递动作范围

每个未被操作性跳过的岗位只执行：

1. 发起沟通；
2. 完整发送已经确定的固定招呼语一次。

不改写招呼语，不发送缩写版本，不发送附件简历，不查看或回复招聘者后续消息。

如果固定招呼语无法完整发送，该岗位记录为失败，不改写或缩短消息后重试。

### 6.7 暂停与继续

用户点击“暂停”后：

1. 页面先显示“正在安全暂停”；
2. 当前正在处理的动作完成或明确记录后，不再开始新动作；
3. 页面进入“已暂停”；
4. 主操作变为“继续投递”；
5. 页面说明“继续后不会重复投递已经处理的岗位”。

用户点击“继续投递”后，从原进度恢复。

### 6.8 验证码和安全验证

出现验证码或安全验证时，产品立即停止新的页面操作并保存当前进度。

页面展示：

- 标题：“请在 Chrome 中完成安全验证”；
- 说明：“助手不会绕过验证码”；
- 状态：“正在检测验证结果”；
- 次操作：“重新打开验证页面”；
- 危险操作：“结束任务”。

用户完成验证后自动恢复，不要求点击“验证码已处理”。

### 6.9 今日沟通上限

检测到 BOSS 今日沟通上限后，任务不可继续。

页面直接进入提前结束报告，展示：

- 标题：“已达到 BOSS 今日沟通上限”；
- 已沟通岗位数；
- 已完成搜索范围；
- 尚未完成搜索范围；
- 停止原因：“平台今日上限”；
- 提示：“明天可重新开始新的投递任务”。

不能将该状态描述为“全部搜索完成”。

当天再次打开产品时继续展示今日上限和部分报告，不再次自动发起投递。

### 6.10 用户结束任务

点击“结束任务”后，先展示确认弹窗：

- 标题：“结束本次投递？”；
- 说明：“将停止继续查找岗位，并生成部分报告”；
- 主操作：“结束并生成报告”；
- 次操作：“继续投递”。

确认后停止新动作，处理当前未确认结果，并生成提前结束报告。

当天再次打开产品时保留该结束结果，不自动恢复。只有用户主动点击“重新开始投递”才创建新的任务。

### 6.11 正常完成

只有所有目标城市和搜索范围均已完整处理后，页面才进入正常完成状态。

页面展示：

- 标题：“本次投递已完成”；
- 搜索范围：“全部完成”；
- 已沟通岗位数；
- 已跳过岗位数；
- 完整报告入口。

正常完成后，当天再次打开产品仍展示本次完成报告，不自动启动第二轮。若用户确实需要再次运行，必须主动点击“再次投递”。

## 7. 用户可见状态

| 状态 | 页面主文案 | 用户是否需要操作 | 离开条件 |
|---|---|---:|---|
| 正在准备 | 正在准备本次投递 | 否 | 自动进入登录或资料读取 |
| 需要 OpenAI 登录 | 需要重新登录 OpenAI | 是 | 检测到授权恢复后自动继续 |
| 需要 BOSS 登录 | 请在 Chrome 中完成 BOSS 登录 | 是 | 检测到登录成功后自动继续 |
| 正在读取简历 | 正在读取 BOSS 在线简历 | 否 | 资料完整或进入资料待完善 |
| 在线简历待完善 | 请先完善 BOSS 在线简历 | 是 | 检测到资料完整后自动继续 |
| 自动投递中 | 正在按你的求职期望投递 | 否 | 暂停、用户结束、阻塞或完成 |
| 正在安全暂停 | 正在完成当前操作 | 否 | 当前操作状态明确后暂停 |
| 已暂停 | 投递任务已暂停 | 可选 | 用户点击继续或结束 |
| 正在结束 | 正在结束本次任务 | 否 | 当前处理结果明确后生成报告 |
| 等待验证码 | 请在 Chrome 中完成安全验证 | 是 | 检测到验证完成后自动继续 |
| 正在恢复 | 已找到上次进度，正在恢复 | 否 | 回到自动投递中 |
| 今日上限 | 已达到 BOSS 今日沟通上限 | 否 | 生成提前结束报告 |
| 安全阻塞 | BOSS 暂停了自动操作 | 否 | 生成提前结束报告，不自动重试 |
| 无法继续 | 本次任务暂时无法继续 | 可选 | 用户重试或结束任务 |
| 正常完成 | 本次投递已完成 | 否 | 用户查看报告或退出 |
| 提前结束 | 本次投递已结束 | 否 | 用户查看部分报告或退出 |

## 8. 异常退出与恢复

### 8.1 关闭或刷新 Web 页面

- 运行中的任务继续运行；
- 已暂停的任务保持暂停；
- 再次打开页面时直接显示当前状态；
- 不创建第二个重复任务。

### 8.2 产品意外退出

同一天再次打开时：

- 自动识别未完成任务和退出前状态；
- 退出前正在运行：页面展示“已找到上次进度，正在恢复”，随后自动恢复；
- 退出前由用户主动暂停：继续显示“已暂停”，等待用户点击继续；
- 已经沟通或已经处理的岗位不会重复执行。

### 8.3 发送结果无法确认

如果退出发生在发送过程中，恢复后不能盲目重发。

用户可见行为：

- 该岗位标记为“发送结果待确认”；
- 本次任务继续处理其他岗位；
- 最终报告单独列出，提醒用户自行核对。

### 8.4 跨日恢复

本次任务不跨天自动继续。

任务运行过程中进入新的一天，或者跨日后再次打开时：

- 原任务立即停止，并以“跨日结束”生成部分报告；
- 用户再次进入产品时重新读取在线简历并创建当天的新任务；
- 不沿用前一天的职位和公司记录。

## 9. 异常处理分级

### 9.1 自动恢复，不打断用户

- 页面短暂加载失败；
- 列表暂时未加载；
- 单个岗位详情打开失败；
- 网络短暂波动。

仅在连续恢复失败后，才进入用户可见的异常状态。

### 9.2 等待用户处理

- OpenAI 授权失效；
- BOSS 登录失效；
- 验证码或安全验证；
- 用户选择自行更新在线简历。

处理完成后自动检测并恢复。

### 9.3 立即停止并生成部分报告

- BOSS 今日沟通上限；
- 明确的反自动化或安全警告；
- 平台明确禁止继续操作；
- 多次恢复后仍无法确认页面状态；
- 用户确认结束任务。

因反自动化或安全警告停止后，当天不提供自动重新开始入口。

## 10. 最终报告

### 10.1 报告顶部摘要

必须展示：

- 本次目标城市；
- 本次意向岗位；
- 本次期望薪资；
- 任务开始与结束时间；
- 正常完成或提前结束；
- 停止原因；
- 搜索范围完成度，例如“6/6 已完成”或“2/6 已完成”；
- 已沟通岗位数；
- 已跳过岗位数；
- 发送结果待确认岗位数。

### 10.2 岗位级记录

每条记录包含：

- 公司；
- 岗位名称；
- 招聘者；
- 招聘者活跃状态；
- 城市；
- 薪资；
- 岗位链接；
- 处理结果；
- 跳过、失败或待确认原因。

### 10.3 报告分组

默认按以下顺序展示：

1. 已沟通；
2. 发送结果待确认；
3. 已跳过或失败。

### 10.4 报告提醒

报告底部固定提醒：

> 后续与招聘者的沟通需要由你本人完成。助手不会自动回复消息，也不会发送附件简历。

### 10.5 报告查看与导出

- 任务结束后，报告页面保持可见，直到用户明确退出；
- 提供“查看完整记录”“复制报告”和“导出报告”；
- 当天再次进入产品时可以继续查看当天最后一次任务结果；
- 产品不提供跨天历史报告库；需要长期保留时由用户主动导出。

## 11. 关键页面文案

### 11.1 运行中

标题：`正在按你的求职期望投递`

说明：`任务会持续运行，只有需要你时才会打断。`

### 11.2 在线简历待完善

标题：`请先完善 BOSS 在线简历`

说明：`意向岗位、目标城市和期望薪资来自 BOSS 在线简历。更新完成后将自动继续。`

### 11.3 BOSS 登录

标题：`请在 Chrome 中完成 BOSS 登录`

说明：`登录成功后将自动继续，不需要返回确认。`

### 11.4 验证码

标题：`请在 Chrome 中完成安全验证`

说明：`助手不会绕过验证码。验证完成后将自动继续。`

### 11.5 暂停

标题：`投递任务已暂停`

说明：`当前进度已保留，继续后不会重复投递。`

### 11.6 今日上限

标题：`已达到 BOSS 今日沟通上限`

说明：`本次任务已提前结束，尚未完成的搜索范围已记录在报告中。`

### 11.7 正常完成

标题：`本次投递已完成`

说明：`所有目标城市和搜索范围均已处理。`

## 12. 验收标准

### 12.1 自动化体验

- 打开产品后无需点击“开始投递”；
- 在线资料完整且登录有效时，不出现任何确认步骤；
- 登录或验证码处理完成后自动恢复；
- 不要求用户输入固定命令或确认口令。
- 当天已有进行中任务时自动恢复，不创建重复任务；
- 当天任务已经结束时不自动启动第二轮。

### 12.2 在线简历资料

- BOSS 在线简历是唯一求职资料来源；
- 助手内不提供城市、薪资或经历的临时补充表单；
- 助手不要求用户重新上传简历；
- 必要字段缺失时一次性列出全部缺失项；
- 用户直接在 BOSS 在线简历中更新；
- 检测到资料完整后自动继续，不要求用户返回确认。

### 12.3 运行页面

- 默认不展示技术日志；
- 默认不逐条刷职位记录；
- 始终可以看到任务状态、总体进度、已沟通数和已跳过数；
- 详细岗位记录由用户按需展开；
- 暂停和结束任务始终可达。

### 12.4 海投边界

- 搜索岗位直接来自 BOSS 在线简历中的意向岗位；
- 搜索结果不再进行二次匹配、评分或相关度分级；
- 不因年限、职级、技术栈、职责、经历相似度或职位详情差异拒绝投递；
- 只因重复、已沟通、公司当天上限、平台限制或无法安全确认结果而不发起沟通；
- 同一公司当天最多沟通 2 个职位；
- 相同或相近岗位优先选择最活跃招聘者；
- 只发送固定招呼语一次；
- 不发送附件简历；
- 不代替用户回复招聘者。

### 12.5 完成与报告

- 不能因为处理完当前可见职位就显示正常完成；
- 正常完成必须覆盖全部目标城市和搜索范围；
- 今日上限、用户停止和异常结束必须生成部分报告；
- 部分报告必须展示未完成范围和停止原因；
- 发送结果不确定的岗位不能自动重发，必须在报告中单独展示；
- 报告生成后结束本次任务，不保留跨任务、跨天记录。

## 13. v1 产品结论

v1 的核心不是提供复杂配置或岗位匹配，而是把用户在 BOSS 在线简历中已经填写的求职期望直接转化为一轮持续海投，让用户打开产品后放心离开。

产品只在真正需要用户完成登录、验证码或完善 BOSS 在线简历时打断；其余时间持续自动投递，并以简洁的 Web 状态页展示进度。任务结束时，以“完整完成”或“提前结束”两种明确结果交付岗位级报告，避免让用户误以为未搜索完的任务已经完成。
