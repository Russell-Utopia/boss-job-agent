# MVP 只实现 Web 工作台

TUI 与三套早期 Web 布局已经通过 throwaway 原型比较，最终选择单一 Web 工作台作为 MVP 界面：岗位页承载运行概况、筛选和处理入口，岗位鉴定、打招呼、在线简历按用户目的分为独立页面，不再设置独立“执行情况”页。策略优化与可选策略验收位于岗位鉴定页，不作为岗位列表操作，也不新增一级导航。原型只用于验证信息架构和交互，不进入生产实现；后台业务接口仍与界面解耦，未来可以增加其他客户端，但 MVP 不同时维护 TUI。
