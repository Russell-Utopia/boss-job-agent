---
status: superseded by ADR-0014
---

# 由 Go 程序拥有任务执行流程

沟通任务的状态、搜索范围遍历、分页或滚动、去重、重试、恢复和完成判断由 Go 程序确定性地管理；Kimi WebBridge 作为首个可替换的浏览器执行器，只负责读取页面、执行浏览器动作并返回结构化结果，未来可以替换为其他浏览器控制或爬取实现。LLM 不拥有任务循环或完成判断，v1 也不把 LLM 作为运行前提；该边界增加了显式状态建模的工作量，但避免浏览器工具或模型的一次调用决定整项任务的生命周期。
