# 允许岗位薪资暂不可用

岗位发现不得因为 BOSS 可见页面无法提供可靠可读薪资而丢弃已具有稳定岗位标识、公司、城市、完整 JD 和可靠平台状态的岗位。薪资在岗位判断内容中是可选值：可靠取得时保存规范化文本，无法可靠取得时保存为空且不得保存不可读字形；`jd_hash` 仍包含这个可选值，因此后来从空值变为可靠薪资会使既有判断进入原有的内容变更处理。界面遇到空薪资时提示“薪资仅可在 BOSS 页面查看”。该决定只取代 ADR-0008 对薪资始终可读的隐含假设，不改变其它岗位身份和判断哈希规则，也不引入字形逆向或 OCR 作为生产依赖。
